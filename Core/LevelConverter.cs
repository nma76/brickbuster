using brickbuster.Models;
using brickbuster.Models.Json;

namespace brickbuster.Core;

public static class LevelConverter
{
    public static LevelDefinition ToDefinition(LevelData levelData)
    {
        // TODO if needed
        return new LevelDefinition();
    }

    public static LevelData ToLevelData(LevelDefinition levelDefinition)
    {
        // TODO if needed
        return new LevelData();
    }
}